{
    "conditioning/01-overlap-circles-small-and-far": {
        "difference": "At scale 1e-3 with offset 1e6, overlapping circles are treated as disjoint. Independent input masks are normalized to viewport units to avoid resvg precision loss; all operations, including division coverage, disagree with their expected regions.",
        "exclusion": "At scale 1e-3 with offset 1e6, overlapping circles are treated as disjoint. Independent input masks are normalized to viewport units to avoid resvg precision loss; all operations, including division coverage, disagree with their expected regions.",
        "fracture": "At scale 1e-3 with offset 1e6, overlapping circles are treated as disjoint. Independent input masks are normalized to viewport units to avoid resvg precision loss; all operations, including division coverage, disagree with their expected regions.",
        "intersection": "At scale 1e-3 with offset 1e6, overlapping circles are treated as disjoint. Independent input masks are normalized to viewport units to avoid resvg precision loss; all operations, including division coverage, disagree with their expected regions.",
        "union": "At scale 1e-3 with offset 1e6, overlapping circles are treated as disjoint. Independent input masks are normalized to viewport units to avoid resvg precision loss; all operations, including division coverage, disagree with their expected regions.",
        "division": "At scale 1e-3 with offset 1e6, overlapping circles are treated as disjoint. Independent input masks are normalized to viewport units to avoid resvg precision loss; all operations, including division coverage, disagree with their expected regions."
    },
    "conditioning/03-edge-shared-full-small-and-far": {
        "difference": "Throws `No outer face of a component found` under the `small-and-far` conditioning \u2014 a scene 2e-3 across sitting at an offset of 1e6, where neighbouring doubles are ~2e-10 apart. Each conditioning on its own passes (`-small` and `-far` are both green), so it is the combination that loses the precision. `config.ts` is explicit that a large offset is a known separate problem that widening tolerances would conceal rather than cure, so this documents the limit rather than proposing to move it. In the production bundle it throws a raw TypeError from `WeakSet.add` in `flagFaces`.",
        "division": "Throws `No outer face of a component found` under the `small-and-far` conditioning \u2014 a scene 2e-3 across sitting at an offset of 1e6, where neighbouring doubles are ~2e-10 apart. Each conditioning on its own passes (`-small` and `-far` are both green), so it is the combination that loses the precision. `config.ts` is explicit that a large offset is a known separate problem that widening tolerances would conceal rather than cure, so this documents the limit rather than proposing to move it. In the production bundle it throws a raw TypeError from `WeakSet.add` in `flagFaces`.",
        "exclusion": "Throws `No outer face of a component found` under the `small-and-far` conditioning \u2014 a scene 2e-3 across sitting at an offset of 1e6, where neighbouring doubles are ~2e-10 apart. Each conditioning on its own passes (`-small` and `-far` are both green), so it is the combination that loses the precision. `config.ts` is explicit that a large offset is a known separate problem that widening tolerances would conceal rather than cure, so this documents the limit rather than proposing to move it. In the production bundle it throws a raw TypeError from `WeakSet.add` in `flagFaces`.",
        "fracture": "Throws `No outer face of a component found` under the `small-and-far` conditioning \u2014 a scene 2e-3 across sitting at an offset of 1e6, where neighbouring doubles are ~2e-10 apart. Each conditioning on its own passes (`-small` and `-far` are both green), so it is the combination that loses the precision. `config.ts` is explicit that a large offset is a known separate problem that widening tolerances would conceal rather than cure, so this documents the limit rather than proposing to move it. In the production bundle it throws a raw TypeError from `WeakSet.add` in `flagFaces`.",
        "intersection": "Throws `No outer face of a component found` under the `small-and-far` conditioning \u2014 a scene 2e-3 across sitting at an offset of 1e6, where neighbouring doubles are ~2e-10 apart. Each conditioning on its own passes (`-small` and `-far` are both green), so it is the combination that loses the precision. `config.ts` is explicit that a large offset is a known separate problem that widening tolerances would conceal rather than cure, so this documents the limit rather than proposing to move it. In the production bundle it throws a raw TypeError from `WeakSet.add` in `flagFaces`.",
        "union": "Throws `No outer face of a component found` under the `small-and-far` conditioning \u2014 a scene 2e-3 across sitting at an offset of 1e6, where neighbouring doubles are ~2e-10 apart. Each conditioning on its own passes (`-small` and `-far` are both green), so it is the combination that loses the precision. `config.ts` is explicit that a large offset is a known separate problem that widening tolerances would conceal rather than cure, so this documents the limit rather than proposing to move it. In the production bundle it throws a raw TypeError from `WeakSet.add` in `flagFaces`."
    },
    "conditioning/04-arc-shared-full-small-and-far": {
        "difference": "Throws `No outer face of a component found` under the `small-and-far` conditioning \u2014 a scene 2e-3 across sitting at an offset of 1e6, where neighbouring doubles are ~2e-10 apart. Each conditioning on its own passes (`-small` and `-far` are both green), so it is the combination that loses the precision. `config.ts` is explicit that a large offset is a known separate problem that widening tolerances would conceal rather than cure, so this documents the limit rather than proposing to move it. In the production bundle it throws a raw TypeError from `WeakSet.add` in `flagFaces`.",
        "division": "Throws `No outer face of a component found` under the `small-and-far` conditioning \u2014 a scene 2e-3 across sitting at an offset of 1e6, where neighbouring doubles are ~2e-10 apart. Each conditioning on its own passes (`-small` and `-far` are both green), so it is the combination that loses the precision. `config.ts` is explicit that a large offset is a known separate problem that widening tolerances would conceal rather than cure, so this documents the limit rather than proposing to move it. In the production bundle it throws a raw TypeError from `WeakSet.add` in `flagFaces`.",
        "exclusion": "Throws `No outer face of a component found` under the `small-and-far` conditioning \u2014 a scene 2e-3 across sitting at an offset of 1e6, where neighbouring doubles are ~2e-10 apart. Each conditioning on its own passes (`-small` and `-far` are both green), so it is the combination that loses the precision. `config.ts` is explicit that a large offset is a known separate problem that widening tolerances would conceal rather than cure, so this documents the limit rather than proposing to move it. In the production bundle it throws a raw TypeError from `WeakSet.add` in `flagFaces`.",
        "fracture": "Throws `No outer face of a component found` under the `small-and-far` conditioning \u2014 a scene 2e-3 across sitting at an offset of 1e6, where neighbouring doubles are ~2e-10 apart. Each conditioning on its own passes (`-small` and `-far` are both green), so it is the combination that loses the precision. `config.ts` is explicit that a large offset is a known separate problem that widening tolerances would conceal rather than cure, so this documents the limit rather than proposing to move it. In the production bundle it throws a raw TypeError from `WeakSet.add` in `flagFaces`.",
        "intersection": "Throws `No outer face of a component found` under the `small-and-far` conditioning \u2014 a scene 2e-3 across sitting at an offset of 1e6, where neighbouring doubles are ~2e-10 apart. Each conditioning on its own passes (`-small` and `-far` are both green), so it is the combination that loses the precision. `config.ts` is explicit that a large offset is a known separate problem that widening tolerances would conceal rather than cure, so this documents the limit rather than proposing to move it. In the production bundle it throws a raw TypeError from `WeakSet.add` in `flagFaces`.",
        "union": "Throws `No outer face of a component found` under the `small-and-far` conditioning \u2014 a scene 2e-3 across sitting at an offset of 1e6, where neighbouring doubles are ~2e-10 apart. Each conditioning on its own passes (`-small` and `-far` are both green), so it is the combination that loses the precision. `config.ts` is explicit that a large offset is a known separate problem that widening tolerances would conceal rather than cure, so this documents the limit rather than proposing to move it. In the production bundle it throws a raw TypeError from `WeakSet.add` in `flagFaces`."
    },
    "malformed/duplicate-opposite-winding-boundary-3": {
        "union": "Opposite-winding copies of A cancel but share an edge with B. The forward result leaves an open boundary and misses the region of B; structural and cleanup-equivalence checks corroborate this raster failure.",
        "exclusion": "Opposite-winding copies of A cancel but share an edge with B. The forward result leaves an open boundary and misses the region of B; structural and cleanup-equivalence checks corroborate this raster failure.",
        "fracture": "Opposite-winding copies of A cancel but share an edge with B. The forward result leaves an open boundary and misses the region of B; structural and cleanup-equivalence checks corroborate this raster failure."
    },
    "malformed/corrected-radius-1e-11": {
        "union": "Positive arc radii at or below NEARLY_LINEAR_EPS (1e-10) are converted to a line before SVG radius correction. The endpoints require a radius-20 semicircle, so sanitization deletes genuine area. The independent raster reference applies mandatory SVG correction before rendering.",
        "difference": "Positive arc radii at or below NEARLY_LINEAR_EPS (1e-10) are converted to a line before SVG radius correction. The endpoints require a radius-20 semicircle, so sanitization deletes genuine area. The independent raster reference applies mandatory SVG correction before rendering.",
        "intersection": "Positive arc radii at or below NEARLY_LINEAR_EPS (1e-10) are converted to a line before SVG radius correction. The endpoints require a radius-20 semicircle, so sanitization deletes genuine area. The independent raster reference applies mandatory SVG correction before rendering.",
        "exclusion": "Positive arc radii at or below NEARLY_LINEAR_EPS (1e-10) are converted to a line before SVG radius correction. The endpoints require a radius-20 semicircle, so sanitization deletes genuine area. The independent raster reference applies mandatory SVG correction before rendering.",
        "division": "Positive arc radii at or below NEARLY_LINEAR_EPS (1e-10) are converted to a line before SVG radius correction. The endpoints require a radius-20 semicircle, so sanitization deletes genuine area. The independent raster reference applies mandatory SVG correction before rendering.",
        "fracture": "Positive arc radii at or below NEARLY_LINEAR_EPS (1e-10) are converted to a line before SVG radius correction. The endpoints require a radius-20 semicircle, so sanitization deletes genuine area. The independent raster reference applies mandatory SVG correction before rendering."
    },
    "malformed/corrected-radius-1e-10": {
        "union": "Positive arc radii at or below NEARLY_LINEAR_EPS (1e-10) are converted to a line before SVG radius correction. The endpoints require a radius-20 semicircle, so sanitization deletes genuine area. The independent raster reference applies mandatory SVG correction before rendering.",
        "difference": "Positive arc radii at or below NEARLY_LINEAR_EPS (1e-10) are converted to a line before SVG radius correction. The endpoints require a radius-20 semicircle, so sanitization deletes genuine area. The independent raster reference applies mandatory SVG correction before rendering.",
        "intersection": "Positive arc radii at or below NEARLY_LINEAR_EPS (1e-10) are converted to a line before SVG radius correction. The endpoints require a radius-20 semicircle, so sanitization deletes genuine area. The independent raster reference applies mandatory SVG correction before rendering.",
        "exclusion": "Positive arc radii at or below NEARLY_LINEAR_EPS (1e-10) are converted to a line before SVG radius correction. The endpoints require a radius-20 semicircle, so sanitization deletes genuine area. The independent raster reference applies mandatory SVG correction before rendering.",
        "division": "Positive arc radii at or below NEARLY_LINEAR_EPS (1e-10) are converted to a line before SVG radius correction. The endpoints require a radius-20 semicircle, so sanitization deletes genuine area. The independent raster reference applies mandatory SVG correction before rendering.",
        "fracture": "Positive arc radii at or below NEARLY_LINEAR_EPS (1e-10) are converted to a line before SVG radius correction. The endpoints require a radius-20 semicircle, so sanitization deletes genuine area. The independent raster reference applies mandatory SVG correction before rendering."
    },
    "malformed/corrected-small-radii-crossing-6": {
        "union": "The tiny conditioning scales positive arc radii to 1e-15, below the fixed NEARLY_LINEAR_EPS cutoff. Sanitization converts the arc to a line before radius correction, deleting the semicircle. This is the same defect as corrected-radius-1e-11 at a smaller scene scale.",
        "difference": "The tiny conditioning scales positive arc radii to 1e-15, below the fixed NEARLY_LINEAR_EPS cutoff. Sanitization converts the arc to a line before radius correction, deleting the semicircle. This is the same defect as corrected-radius-1e-11 at a smaller scene scale.",
        "intersection": "The tiny conditioning scales positive arc radii to 1e-15, below the fixed NEARLY_LINEAR_EPS cutoff. Sanitization converts the arc to a line before radius correction, deleting the semicircle. This is the same defect as corrected-radius-1e-11 at a smaller scene scale.",
        "exclusion": "The tiny conditioning scales positive arc radii to 1e-15, below the fixed NEARLY_LINEAR_EPS cutoff. Sanitization converts the arc to a line before radius correction, deleting the semicircle. This is the same defect as corrected-radius-1e-11 at a smaller scene scale.",
        "division": "The tiny conditioning scales positive arc radii to 1e-15, below the fixed NEARLY_LINEAR_EPS cutoff. Sanitization converts the arc to a line before radius correction, deleting the semicircle. This is the same defect as corrected-radius-1e-11 at a smaller scene scale.",
        "fracture": "The tiny conditioning scales positive arc radii to 1e-15, below the fixed NEARLY_LINEAR_EPS cutoff. Sanitization converts the arc to a line before radius correction, deleting the semicircle. This is the same defect as corrected-radius-1e-11 at a smaller scene scale."
    }
}
