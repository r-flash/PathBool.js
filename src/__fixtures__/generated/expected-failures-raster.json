{
    "coincident/04-arc-vs-cubic-circle": {
        "difference": "Correct now, but slow: ~9.4s per operation, all of it in intersection finding. The arc and the cubic approximating it stay within a whisker of each other along a whole quadrant, so their boxes overlap everywhere and the subdivision explores an enormous number of leaf pairs to return 3 points per pair. Needs the finder to recognize that two curves run together within tolerance over a stretch, rather than bisecting its way along it.",
        "division": "Correct now, but slow: ~9.4s per operation, all of it in intersection finding. The arc and the cubic approximating it stay within a whisker of each other along a whole quadrant, so their boxes overlap everywhere and the subdivision explores an enormous number of leaf pairs to return 3 points per pair. Needs the finder to recognize that two curves run together within tolerance over a stretch, rather than bisecting its way along it.",
        "exclusion": "Correct now, but slow: ~9.4s per operation, all of it in intersection finding. The arc and the cubic approximating it stay within a whisker of each other along a whole quadrant, so their boxes overlap everywhere and the subdivision explores an enormous number of leaf pairs to return 3 points per pair. Needs the finder to recognize that two curves run together within tolerance over a stretch, rather than bisecting its way along it.",
        "fracture": "Correct now, but slow: ~9.4s per operation, all of it in intersection finding. The arc and the cubic approximating it stay within a whisker of each other along a whole quadrant, so their boxes overlap everywhere and the subdivision explores an enormous number of leaf pairs to return 3 points per pair. Needs the finder to recognize that two curves run together within tolerance over a stretch, rather than bisecting its way along it.",
        "intersection": "Correct now, but slow: ~9.4s per operation, all of it in intersection finding. The arc and the cubic approximating it stay within a whisker of each other along a whole quadrant, so their boxes overlap everywhere and the subdivision explores an enormous number of leaf pairs to return 3 points per pair. Needs the finder to recognize that two curves run together within tolerance over a stretch, rather than bisecting its way along it.",
        "union": "Correct now, but slow: ~9.4s per operation, all of it in intersection finding. The arc and the cubic approximating it stay within a whisker of each other along a whole quadrant, so their boxes overlap everywhere and the subdivision explores an enormous number of leaf pairs to return 3 points per pair. Needs the finder to recognize that two curves run together within tolerance over a stretch, rather than bisecting its way along it."
    },
    "coincident/07-identical-circle-rotated-start": {
        "difference": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "division": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "exclusion": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "fracture": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "intersection": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "union": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead."
    },
    "coincident/10-square-vs-degenerate-curves": {
        "difference": "Throws `No outer face of a component found`. A and B are the same square, but B is drawn with segments that are degenerate as curves — a zero-length cubic, a cubic whose controls are collinear with its endpoints, a quadratic whose control sits on its chord, or an arc of zero radius. Those segments are never normalised to lines, so the coincident edges do not merge and the dual graph is left without a traceable outer face. In the production bundle the stripped assert turns this into a raw TypeError.",
        "division": "Throws `No outer face of a component found`. A and B are the same square, but B is drawn with segments that are degenerate as curves — a zero-length cubic, a cubic whose controls are collinear with its endpoints, a quadratic whose control sits on its chord, or an arc of zero radius. Those segments are never normalised to lines, so the coincident edges do not merge and the dual graph is left without a traceable outer face. In the production bundle the stripped assert turns this into a raw TypeError.",
        "exclusion": "Throws `No outer face of a component found`. A and B are the same square, but B is drawn with segments that are degenerate as curves — a zero-length cubic, a cubic whose controls are collinear with its endpoints, a quadratic whose control sits on its chord, or an arc of zero radius. Those segments are never normalised to lines, so the coincident edges do not merge and the dual graph is left without a traceable outer face. In the production bundle the stripped assert turns this into a raw TypeError.",
        "fracture": "Throws `No outer face of a component found`. A and B are the same square, but B is drawn with segments that are degenerate as curves — a zero-length cubic, a cubic whose controls are collinear with its endpoints, a quadratic whose control sits on its chord, or an arc of zero radius. Those segments are never normalised to lines, so the coincident edges do not merge and the dual graph is left without a traceable outer face. In the production bundle the stripped assert turns this into a raw TypeError.",
        "intersection": "Throws `No outer face of a component found`. A and B are the same square, but B is drawn with segments that are degenerate as curves — a zero-length cubic, a cubic whose controls are collinear with its endpoints, a quadratic whose control sits on its chord, or an arc of zero radius. Those segments are never normalised to lines, so the coincident edges do not merge and the dual graph is left without a traceable outer face. In the production bundle the stripped assert turns this into a raw TypeError.",
        "union": "Throws `No outer face of a component found`. A and B are the same square, but B is drawn with segments that are degenerate as curves — a zero-length cubic, a cubic whose controls are collinear with its endpoints, a quadratic whose control sits on its chord, or an arc of zero radius. Those segments are never normalised to lines, so the coincident edges do not merge and the dual graph is left without a traceable outer face. In the production bundle the stripped assert turns this into a raw TypeError."
    },
    "coincident/11-circle-vs-overlong-radii": {
        "difference": "Over the 5s budget (5.4s to 21.2s per operation), all of it in intersection finding, and for the same reason as `coincident/04-arc-vs-cubic-circle`: two curves run together within tolerance over a whole stretch, so their boxes overlap everywhere and the subdivision bisects its way along them. Correctness is not assessed here because the budget check short-circuits ahead of it — but Fracture returns a single face for what should be two (the disc and the wedge sharing the rim), so the shared curved boundary is being dissolved rather than kept as an edge. Same root cause as `touching/09-arc-shared-full`.",
        "division": "Over the 5s budget (5.4s to 21.2s per operation), all of it in intersection finding, and for the same reason as `coincident/04-arc-vs-cubic-circle`: two curves run together within tolerance over a whole stretch, so their boxes overlap everywhere and the subdivision bisects its way along them. Correctness is not assessed here because the budget check short-circuits ahead of it — but Fracture returns a single face for what should be two (the disc and the wedge sharing the rim), so the shared curved boundary is being dissolved rather than kept as an edge. Same root cause as `touching/09-arc-shared-full`.",
        "exclusion": "Over the 5s budget (5.4s to 21.2s per operation), all of it in intersection finding, and for the same reason as `coincident/04-arc-vs-cubic-circle`: two curves run together within tolerance over a whole stretch, so their boxes overlap everywhere and the subdivision bisects its way along them. Correctness is not assessed here because the budget check short-circuits ahead of it — but Fracture returns a single face for what should be two (the disc and the wedge sharing the rim), so the shared curved boundary is being dissolved rather than kept as an edge. Same root cause as `touching/09-arc-shared-full`.",
        "fracture": "Over the 5s budget (5.4s to 21.2s per operation), all of it in intersection finding, and for the same reason as `coincident/04-arc-vs-cubic-circle`: two curves run together within tolerance over a whole stretch, so their boxes overlap everywhere and the subdivision bisects its way along them. Correctness is not assessed here because the budget check short-circuits ahead of it — but Fracture returns a single face for what should be two (the disc and the wedge sharing the rim), so the shared curved boundary is being dissolved rather than kept as an edge. Same root cause as `touching/09-arc-shared-full`.",
        "intersection": "Over the 5s budget (5.4s to 21.2s per operation), all of it in intersection finding, and for the same reason as `coincident/04-arc-vs-cubic-circle`: two curves run together within tolerance over a whole stretch, so their boxes overlap everywhere and the subdivision bisects its way along them. Correctness is not assessed here because the budget check short-circuits ahead of it — but Fracture returns a single face for what should be two (the disc and the wedge sharing the rim), so the shared curved boundary is being dissolved rather than kept as an edge. Same root cause as `touching/09-arc-shared-full`.",
        "union": "Over the 5s budget (5.4s to 21.2s per operation), all of it in intersection finding, and for the same reason as `coincident/04-arc-vs-cubic-circle`: two curves run together within tolerance over a whole stretch, so their boxes overlap everywhere and the subdivision bisects its way along them. Correctness is not assessed here because the budget check short-circuits ahead of it — but Fracture returns a single face for what should be two (the disc and the wedge sharing the rim), so the shared curved boundary is being dissolved rather than kept as an edge. Same root cause as `touching/09-arc-shared-full`."
    },
    "coincident/12-square-vs-zero-radius-arc": {
        "difference": "Throws `No outer face of a component found`. A and B are the same square, but B is drawn with segments that are degenerate as curves — a zero-length cubic, a cubic whose controls are collinear with its endpoints, a quadratic whose control sits on its chord, or an arc of zero radius. Those segments are never normalised to lines, so the coincident edges do not merge and the dual graph is left without a traceable outer face. In the production bundle the stripped assert turns this into a raw TypeError.",
        "division": "Throws `No outer face of a component found`. A and B are the same square, but B is drawn with segments that are degenerate as curves — a zero-length cubic, a cubic whose controls are collinear with its endpoints, a quadratic whose control sits on its chord, or an arc of zero radius. Those segments are never normalised to lines, so the coincident edges do not merge and the dual graph is left without a traceable outer face. In the production bundle the stripped assert turns this into a raw TypeError.",
        "exclusion": "Throws `No outer face of a component found`. A and B are the same square, but B is drawn with segments that are degenerate as curves — a zero-length cubic, a cubic whose controls are collinear with its endpoints, a quadratic whose control sits on its chord, or an arc of zero radius. Those segments are never normalised to lines, so the coincident edges do not merge and the dual graph is left without a traceable outer face. In the production bundle the stripped assert turns this into a raw TypeError.",
        "fracture": "Throws `No outer face of a component found`. A and B are the same square, but B is drawn with segments that are degenerate as curves — a zero-length cubic, a cubic whose controls are collinear with its endpoints, a quadratic whose control sits on its chord, or an arc of zero radius. Those segments are never normalised to lines, so the coincident edges do not merge and the dual graph is left without a traceable outer face. In the production bundle the stripped assert turns this into a raw TypeError.",
        "intersection": "Throws `No outer face of a component found`. A and B are the same square, but B is drawn with segments that are degenerate as curves — a zero-length cubic, a cubic whose controls are collinear with its endpoints, a quadratic whose control sits on its chord, or an arc of zero radius. Those segments are never normalised to lines, so the coincident edges do not merge and the dual graph is left without a traceable outer face. In the production bundle the stripped assert turns this into a raw TypeError.",
        "union": "Throws `No outer face of a component found`. A and B are the same square, but B is drawn with segments that are degenerate as curves — a zero-length cubic, a cubic whose controls are collinear with its endpoints, a quadratic whose control sits on its chord, or an arc of zero radius. Those segments are never normalised to lines, so the coincident edges do not merge and the dual graph is left without a traceable outer face. In the production bundle the stripped assert turns this into a raw TypeError."
    },
    "conditioning/01-overlap-circles-small-and-far": {
        "difference": "Two overlapping circles are treated as disjoint under the `small-and-far` conditioning (a scene 2e-3 across at an offset of 1e6). |A u B| comes out as 3.14e-6, one circle, while the three parts sum to 9.42e-6, three circles; the raster oracle disagrees on 7% of compared pixels. Same precision limit as `conditioning/03-edge-shared-full-small-and-far`, and the same note applies: each conditioning passes on its own.",
        "exclusion": "Two overlapping circles are treated as disjoint under the `small-and-far` conditioning (a scene 2e-3 across at an offset of 1e6). |A u B| comes out as 3.14e-6, one circle, while the three parts sum to 9.42e-6, three circles; the raster oracle disagrees on 7% of compared pixels. Same precision limit as `conditioning/03-edge-shared-full-small-and-far`, and the same note applies: each conditioning passes on its own.",
        "fracture": "Two overlapping circles are treated as disjoint under the `small-and-far` conditioning (a scene 2e-3 across at an offset of 1e6). |A u B| comes out as 3.14e-6, one circle, while the three parts sum to 9.42e-6, three circles; the raster oracle disagrees on 7% of compared pixels. Same precision limit as `conditioning/03-edge-shared-full-small-and-far`, and the same note applies: each conditioning passes on its own.",
        "intersection": "Two overlapping circles are treated as disjoint under the `small-and-far` conditioning (a scene 2e-3 across at an offset of 1e6). |A u B| comes out as 3.14e-6, one circle, while the three parts sum to 9.42e-6, three circles; the raster oracle disagrees on 7% of compared pixels. Same precision limit as `conditioning/03-edge-shared-full-small-and-far`, and the same note applies: each conditioning passes on its own.",
        "union": "Two overlapping circles are treated as disjoint under the `small-and-far` conditioning (a scene 2e-3 across at an offset of 1e6). |A u B| comes out as 3.14e-6, one circle, while the three parts sum to 9.42e-6, three circles; the raster oracle disagrees on 7% of compared pixels. Same precision limit as `conditioning/03-edge-shared-full-small-and-far`, and the same note applies: each conditioning passes on its own."
    },
    "conditioning/01-overlap-circles-tiny": {
        "difference": "Wrong by 0.45% to 0.90% of compared pixels at the `tiny` conditioning, where the scene is 2e-6 across — the size of the *unscaled* `EPS.point`. `epsilonsForExtent` only ever scales the tolerances down from a 50-unit reference, so at this extent they are around 4e-14 and the intersection points land further apart than that.",
        "division": "Wrong by 0.45% to 0.90% of compared pixels at the `tiny` conditioning, where the scene is 2e-6 across — the size of the *unscaled* `EPS.point`. `epsilonsForExtent` only ever scales the tolerances down from a 50-unit reference, so at this extent they are around 4e-14 and the intersection points land further apart than that.",
        "exclusion": "Wrong by 0.45% to 0.90% of compared pixels at the `tiny` conditioning, where the scene is 2e-6 across — the size of the *unscaled* `EPS.point`. `epsilonsForExtent` only ever scales the tolerances down from a 50-unit reference, so at this extent they are around 4e-14 and the intersection points land further apart than that.",
        "fracture": "Wrong by 0.45% to 0.90% of compared pixels at the `tiny` conditioning, where the scene is 2e-6 across — the size of the *unscaled* `EPS.point`. `epsilonsForExtent` only ever scales the tolerances down from a 50-unit reference, so at this extent they are around 4e-14 and the intersection points land further apart than that.",
        "intersection": "Wrong by 0.45% to 0.90% of compared pixels at the `tiny` conditioning, where the scene is 2e-6 across — the size of the *unscaled* `EPS.point`. `epsilonsForExtent` only ever scales the tolerances down from a 50-unit reference, so at this extent they are around 4e-14 and the intersection points land further apart than that.",
        "union": "Wrong by 0.45% to 0.90% of compared pixels at the `tiny` conditioning, where the scene is 2e-6 across — the size of the *unscaled* `EPS.point`. `epsilonsForExtent` only ever scales the tolerances down from a 50-unit reference, so at this extent they are around 4e-14 and the intersection points land further apart than that."
    },
    "conditioning/03-edge-shared-full-small-and-far": {
        "difference": "Throws `No outer face of a component found` under the `small-and-far` conditioning — a scene 2e-3 across sitting at an offset of 1e6, where neighbouring doubles are ~2e-10 apart. Each conditioning on its own passes (`-small` and `-far` are both green), so it is the combination that loses the precision. `config.ts` is explicit that a large offset is a known separate problem that widening tolerances would conceal rather than cure, so this documents the limit rather than proposing to move it. In the production bundle it throws a raw TypeError from `WeakSet.add` in `flagFaces`.",
        "division": "Throws `No outer face of a component found` under the `small-and-far` conditioning — a scene 2e-3 across sitting at an offset of 1e6, where neighbouring doubles are ~2e-10 apart. Each conditioning on its own passes (`-small` and `-far` are both green), so it is the combination that loses the precision. `config.ts` is explicit that a large offset is a known separate problem that widening tolerances would conceal rather than cure, so this documents the limit rather than proposing to move it. In the production bundle it throws a raw TypeError from `WeakSet.add` in `flagFaces`.",
        "exclusion": "Throws `No outer face of a component found` under the `small-and-far` conditioning — a scene 2e-3 across sitting at an offset of 1e6, where neighbouring doubles are ~2e-10 apart. Each conditioning on its own passes (`-small` and `-far` are both green), so it is the combination that loses the precision. `config.ts` is explicit that a large offset is a known separate problem that widening tolerances would conceal rather than cure, so this documents the limit rather than proposing to move it. In the production bundle it throws a raw TypeError from `WeakSet.add` in `flagFaces`.",
        "fracture": "Throws `No outer face of a component found` under the `small-and-far` conditioning — a scene 2e-3 across sitting at an offset of 1e6, where neighbouring doubles are ~2e-10 apart. Each conditioning on its own passes (`-small` and `-far` are both green), so it is the combination that loses the precision. `config.ts` is explicit that a large offset is a known separate problem that widening tolerances would conceal rather than cure, so this documents the limit rather than proposing to move it. In the production bundle it throws a raw TypeError from `WeakSet.add` in `flagFaces`.",
        "intersection": "Throws `No outer face of a component found` under the `small-and-far` conditioning — a scene 2e-3 across sitting at an offset of 1e6, where neighbouring doubles are ~2e-10 apart. Each conditioning on its own passes (`-small` and `-far` are both green), so it is the combination that loses the precision. `config.ts` is explicit that a large offset is a known separate problem that widening tolerances would conceal rather than cure, so this documents the limit rather than proposing to move it. In the production bundle it throws a raw TypeError from `WeakSet.add` in `flagFaces`.",
        "union": "Throws `No outer face of a component found` under the `small-and-far` conditioning — a scene 2e-3 across sitting at an offset of 1e6, where neighbouring doubles are ~2e-10 apart. Each conditioning on its own passes (`-small` and `-far` are both green), so it is the combination that loses the precision. `config.ts` is explicit that a large offset is a known separate problem that widening tolerances would conceal rather than cure, so this documents the limit rather than proposing to move it. In the production bundle it throws a raw TypeError from `WeakSet.add` in `flagFaces`."
    },
    "conditioning/04-arc-shared-full-far": {
        "difference": "Over the 5s budget (5.4s to 21.2s per operation), all of it in intersection finding, and for the same reason as `coincident/04-arc-vs-cubic-circle`: two curves run together within tolerance over a whole stretch, so their boxes overlap everywhere and the subdivision bisects its way along them. Correctness is not assessed here because the budget check short-circuits ahead of it — but Fracture returns a single face for what should be two (the disc and the wedge sharing the rim), so the shared curved boundary is being dissolved rather than kept as an edge. Same root cause as `touching/09-arc-shared-full`.",
        "division": "Over the 5s budget (5.4s to 21.2s per operation), all of it in intersection finding, and for the same reason as `coincident/04-arc-vs-cubic-circle`: two curves run together within tolerance over a whole stretch, so their boxes overlap everywhere and the subdivision bisects its way along them. Correctness is not assessed here because the budget check short-circuits ahead of it — but Fracture returns a single face for what should be two (the disc and the wedge sharing the rim), so the shared curved boundary is being dissolved rather than kept as an edge. Same root cause as `touching/09-arc-shared-full`.",
        "exclusion": "Over the 5s budget (5.4s to 21.2s per operation), all of it in intersection finding, and for the same reason as `coincident/04-arc-vs-cubic-circle`: two curves run together within tolerance over a whole stretch, so their boxes overlap everywhere and the subdivision bisects its way along them. Correctness is not assessed here because the budget check short-circuits ahead of it — but Fracture returns a single face for what should be two (the disc and the wedge sharing the rim), so the shared curved boundary is being dissolved rather than kept as an edge. Same root cause as `touching/09-arc-shared-full`.",
        "fracture": "Over the 5s budget (5.4s to 21.2s per operation), all of it in intersection finding, and for the same reason as `coincident/04-arc-vs-cubic-circle`: two curves run together within tolerance over a whole stretch, so their boxes overlap everywhere and the subdivision bisects its way along them. Correctness is not assessed here because the budget check short-circuits ahead of it — but Fracture returns a single face for what should be two (the disc and the wedge sharing the rim), so the shared curved boundary is being dissolved rather than kept as an edge. Same root cause as `touching/09-arc-shared-full`.",
        "intersection": "Over the 5s budget (5.4s to 21.2s per operation), all of it in intersection finding, and for the same reason as `coincident/04-arc-vs-cubic-circle`: two curves run together within tolerance over a whole stretch, so their boxes overlap everywhere and the subdivision bisects its way along them. Correctness is not assessed here because the budget check short-circuits ahead of it — but Fracture returns a single face for what should be two (the disc and the wedge sharing the rim), so the shared curved boundary is being dissolved rather than kept as an edge. Same root cause as `touching/09-arc-shared-full`.",
        "union": "Over the 5s budget (5.4s to 21.2s per operation), all of it in intersection finding, and for the same reason as `coincident/04-arc-vs-cubic-circle`: two curves run together within tolerance over a whole stretch, so their boxes overlap everywhere and the subdivision bisects its way along them. Correctness is not assessed here because the budget check short-circuits ahead of it — but Fracture returns a single face for what should be two (the disc and the wedge sharing the rim), so the shared curved boundary is being dissolved rather than kept as an edge. Same root cause as `touching/09-arc-shared-full`."
    },
    "conditioning/04-arc-shared-full-large": {
        "difference": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "division": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "exclusion": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "fracture": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "intersection": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "union": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead."
    },
    "conditioning/04-arc-shared-full-rotated": {
        "difference": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "division": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "exclusion": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "fracture": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "intersection": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "union": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead."
    },
    "conditioning/04-arc-shared-full-small": {
        "difference": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "division": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "exclusion": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "fracture": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "intersection": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "union": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead."
    },
    "conditioning/04-arc-shared-full-small-and-far": {
        "difference": "Throws `No outer face of a component found` under the `small-and-far` conditioning — a scene 2e-3 across sitting at an offset of 1e6, where neighbouring doubles are ~2e-10 apart. Each conditioning on its own passes (`-small` and `-far` are both green), so it is the combination that loses the precision. `config.ts` is explicit that a large offset is a known separate problem that widening tolerances would conceal rather than cure, so this documents the limit rather than proposing to move it. In the production bundle it throws a raw TypeError from `WeakSet.add` in `flagFaces`.",
        "division": "Throws `No outer face of a component found` under the `small-and-far` conditioning — a scene 2e-3 across sitting at an offset of 1e6, where neighbouring doubles are ~2e-10 apart. Each conditioning on its own passes (`-small` and `-far` are both green), so it is the combination that loses the precision. `config.ts` is explicit that a large offset is a known separate problem that widening tolerances would conceal rather than cure, so this documents the limit rather than proposing to move it. In the production bundle it throws a raw TypeError from `WeakSet.add` in `flagFaces`.",
        "exclusion": "Throws `No outer face of a component found` under the `small-and-far` conditioning — a scene 2e-3 across sitting at an offset of 1e6, where neighbouring doubles are ~2e-10 apart. Each conditioning on its own passes (`-small` and `-far` are both green), so it is the combination that loses the precision. `config.ts` is explicit that a large offset is a known separate problem that widening tolerances would conceal rather than cure, so this documents the limit rather than proposing to move it. In the production bundle it throws a raw TypeError from `WeakSet.add` in `flagFaces`.",
        "fracture": "Throws `No outer face of a component found` under the `small-and-far` conditioning — a scene 2e-3 across sitting at an offset of 1e6, where neighbouring doubles are ~2e-10 apart. Each conditioning on its own passes (`-small` and `-far` are both green), so it is the combination that loses the precision. `config.ts` is explicit that a large offset is a known separate problem that widening tolerances would conceal rather than cure, so this documents the limit rather than proposing to move it. In the production bundle it throws a raw TypeError from `WeakSet.add` in `flagFaces`.",
        "intersection": "Throws `No outer face of a component found` under the `small-and-far` conditioning — a scene 2e-3 across sitting at an offset of 1e6, where neighbouring doubles are ~2e-10 apart. Each conditioning on its own passes (`-small` and `-far` are both green), so it is the combination that loses the precision. `config.ts` is explicit that a large offset is a known separate problem that widening tolerances would conceal rather than cure, so this documents the limit rather than proposing to move it. In the production bundle it throws a raw TypeError from `WeakSet.add` in `flagFaces`.",
        "union": "Throws `No outer face of a component found` under the `small-and-far` conditioning — a scene 2e-3 across sitting at an offset of 1e6, where neighbouring doubles are ~2e-10 apart. Each conditioning on its own passes (`-small` and `-far` are both green), so it is the combination that loses the precision. `config.ts` is explicit that a large offset is a known separate problem that widening tolerances would conceal rather than cure, so this documents the limit rather than proposing to move it. In the production bundle it throws a raw TypeError from `WeakSet.add` in `flagFaces`."
    },
    "conditioning/04-arc-shared-full-tiny": {
        "difference": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "division": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "exclusion": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "fracture": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "intersection": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "union": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead."
    },
    "fill-rule/10-double-circle-x-square-evenodd": {
        "difference": "The even-odd rule is not applied to a subpath that winds twice. A is the unit circle traversed twice round in one subpath, so under even-odd it is empty and Union should be B alone. Instead every operation returns exactly what the non-zero reading gives (`fill-rule/09` and `fill-rule/10` produce identical output — 3 Fracture faces, the same pixels), disagreeing with resvg on 11% to 17% of compared pixels. The self-overlap is resolved before the fill rule is consulted, so the winding count of 2 never reaches `getFlag`.",
        "division": "The even-odd rule is not applied to a subpath that winds twice. A is the unit circle traversed twice round in one subpath, so under even-odd it is empty and Union should be B alone. Instead every operation returns exactly what the non-zero reading gives (`fill-rule/09` and `fill-rule/10` produce identical output — 3 Fracture faces, the same pixels), disagreeing with resvg on 11% to 17% of compared pixels. The self-overlap is resolved before the fill rule is consulted, so the winding count of 2 never reaches `getFlag`.",
        "exclusion": "The even-odd rule is not applied to a subpath that winds twice. A is the unit circle traversed twice round in one subpath, so under even-odd it is empty and Union should be B alone. Instead every operation returns exactly what the non-zero reading gives (`fill-rule/09` and `fill-rule/10` produce identical output — 3 Fracture faces, the same pixels), disagreeing with resvg on 11% to 17% of compared pixels. The self-overlap is resolved before the fill rule is consulted, so the winding count of 2 never reaches `getFlag`.",
        "fracture": "The even-odd rule is not applied to a subpath that winds twice. A is the unit circle traversed twice round in one subpath, so under even-odd it is empty and Union should be B alone. Instead every operation returns exactly what the non-zero reading gives (`fill-rule/09` and `fill-rule/10` produce identical output — 3 Fracture faces, the same pixels), disagreeing with resvg on 11% to 17% of compared pixels. The self-overlap is resolved before the fill rule is consulted, so the winding count of 2 never reaches `getFlag`.",
        "intersection": "The even-odd rule is not applied to a subpath that winds twice. A is the unit circle traversed twice round in one subpath, so under even-odd it is empty and Union should be B alone. Instead every operation returns exactly what the non-zero reading gives (`fill-rule/09` and `fill-rule/10` produce identical output — 3 Fracture faces, the same pixels), disagreeing with resvg on 11% to 17% of compared pixels. The self-overlap is resolved before the fill rule is consulted, so the winding count of 2 never reaches `getFlag`.",
        "union": "The even-odd rule is not applied to a subpath that winds twice. A is the unit circle traversed twice round in one subpath, so under even-odd it is empty and Union should be B alone. Instead every operation returns exactly what the non-zero reading gives (`fill-rule/09` and `fill-rule/10` produce identical output — 3 Fracture faces, the same pixels), disagreeing with resvg on 11% to 17% of compared pixels. The self-overlap is resolved before the fill rule is consulted, so the winding count of 2 never reaches `getFlag`."
    },
    "touching/08-arc-shared-partial": {
        "difference": "Over the 5s budget (5.4s to 21.2s per operation), all of it in intersection finding, and for the same reason as `coincident/04-arc-vs-cubic-circle`: two curves run together within tolerance over a whole stretch, so their boxes overlap everywhere and the subdivision bisects its way along them. Correctness is not assessed here because the budget check short-circuits ahead of it — but Fracture returns a single face for what should be two (the disc and the wedge sharing the rim), so the shared curved boundary is being dissolved rather than kept as an edge. Same root cause as `touching/09-arc-shared-full`.",
        "division": "Over the 5s budget (5.4s to 21.2s per operation), all of it in intersection finding, and for the same reason as `coincident/04-arc-vs-cubic-circle`: two curves run together within tolerance over a whole stretch, so their boxes overlap everywhere and the subdivision bisects its way along them. Correctness is not assessed here because the budget check short-circuits ahead of it — but Fracture returns a single face for what should be two (the disc and the wedge sharing the rim), so the shared curved boundary is being dissolved rather than kept as an edge. Same root cause as `touching/09-arc-shared-full`.",
        "exclusion": "Over the 5s budget (5.4s to 21.2s per operation), all of it in intersection finding, and for the same reason as `coincident/04-arc-vs-cubic-circle`: two curves run together within tolerance over a whole stretch, so their boxes overlap everywhere and the subdivision bisects its way along them. Correctness is not assessed here because the budget check short-circuits ahead of it — but Fracture returns a single face for what should be two (the disc and the wedge sharing the rim), so the shared curved boundary is being dissolved rather than kept as an edge. Same root cause as `touching/09-arc-shared-full`.",
        "fracture": "Over the 5s budget (5.4s to 21.2s per operation), all of it in intersection finding, and for the same reason as `coincident/04-arc-vs-cubic-circle`: two curves run together within tolerance over a whole stretch, so their boxes overlap everywhere and the subdivision bisects its way along them. Correctness is not assessed here because the budget check short-circuits ahead of it — but Fracture returns a single face for what should be two (the disc and the wedge sharing the rim), so the shared curved boundary is being dissolved rather than kept as an edge. Same root cause as `touching/09-arc-shared-full`.",
        "intersection": "Over the 5s budget (5.4s to 21.2s per operation), all of it in intersection finding, and for the same reason as `coincident/04-arc-vs-cubic-circle`: two curves run together within tolerance over a whole stretch, so their boxes overlap everywhere and the subdivision bisects its way along them. Correctness is not assessed here because the budget check short-circuits ahead of it — but Fracture returns a single face for what should be two (the disc and the wedge sharing the rim), so the shared curved boundary is being dissolved rather than kept as an edge. Same root cause as `touching/09-arc-shared-full`.",
        "union": "Over the 5s budget (5.4s to 21.2s per operation), all of it in intersection finding, and for the same reason as `coincident/04-arc-vs-cubic-circle`: two curves run together within tolerance over a whole stretch, so their boxes overlap everywhere and the subdivision bisects its way along them. Correctness is not assessed here because the budget check short-circuits ahead of it — but Fracture returns a single face for what should be two (the disc and the wedge sharing the rim), so the shared curved boundary is being dissolved rather than kept as an edge. Same root cause as `touching/09-arc-shared-full`."
    },
    "touching/09-arc-shared-full": {
        "difference": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "division": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "exclusion": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "fracture": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "intersection": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "union": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead."
    }
}
