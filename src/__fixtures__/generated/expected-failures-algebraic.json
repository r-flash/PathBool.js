{
    "coincident/04-arc-vs-cubic-circle": {
        "commutative-exclusion": "Correct now, but slow: ~9.4s per operation, all of it in intersection finding. The arc and the cubic approximating it stay within a whisker of each other along a whole quadrant, so their boxes overlap everywhere and the subdivision explores an enormous number of leaf pairs to return 3 points per pair. Needs the finder to recognize that two curves run together within tolerance over a stretch, rather than bisecting its way along it.",
        "commutative-fracture": "Correct now, but slow: ~9.4s per operation, all of it in intersection finding. The arc and the cubic approximating it stay within a whisker of each other along a whole quadrant, so their boxes overlap everywhere and the subdivision explores an enormous number of leaf pairs to return 3 points per pair. Needs the finder to recognize that two curves run together within tolerance over a stretch, rather than bisecting its way along it.",
        "commutative-intersection": "Correct now, but slow: ~9.4s per operation, all of it in intersection finding. The arc and the cubic approximating it stay within a whisker of each other along a whole quadrant, so their boxes overlap everywhere and the subdivision explores an enormous number of leaf pairs to return 3 points per pair. Needs the finder to recognize that two curves run together within tolerance over a stretch, rather than bisecting its way along it.",
        "commutative-union": "Correct now, but slow: ~9.4s per operation, all of it in intersection finding. The arc and the cubic approximating it stay within a whisker of each other along a whole quadrant, so their boxes overlap everywhere and the subdivision explores an enormous number of leaf pairs to return 3 points per pair. Needs the finder to recognize that two curves run together within tolerance over a stretch, rather than bisecting its way along it.",
        "division-tiles-a": "Correct now, but slow: ~9.4s per operation, all of it in intersection finding. The arc and the cubic approximating it stay within a whisker of each other along a whole quadrant, so their boxes overlap everywhere and the subdivision explores an enormous number of leaf pairs to return 3 points per pair. Needs the finder to recognize that two curves run together within tolerance over a stretch, rather than bisecting its way along it.",
        "exclusion-is-union-minus-intersection": "Correct now, but slow: ~9.4s per operation, all of it in intersection finding. The arc and the cubic approximating it stay within a whisker of each other along a whole quadrant, so their boxes overlap everywhere and the subdivision explores an enormous number of leaf pairs to return 3 points per pair. Needs the finder to recognize that two curves run together within tolerance over a stretch, rather than bisecting its way along it.",
        "fracture-tiles-union": "Correct now, but slow: ~9.4s per operation, all of it in intersection finding. The arc and the cubic approximating it stay within a whisker of each other along a whole quadrant, so their boxes overlap everywhere and the subdivision explores an enormous number of leaf pairs to return 3 points per pair. Needs the finder to recognize that two curves run together within tolerance over a stretch, rather than bisecting its way along it.",
        "partition-orientation": "Correct now, but slow: ~9.4s per operation, all of it in intersection finding. The arc and the cubic approximating it stay within a whisker of each other along a whole quadrant, so their boxes overlap everywhere and the subdivision explores an enormous number of leaf pairs to return 3 points per pair. Needs the finder to recognize that two curves run together within tolerance over a stretch, rather than bisecting its way along it.",
        "parts-fit-inside-the-union": "Correct now, but slow: ~9.4s per operation, all of it in intersection finding. The arc and the cubic approximating it stay within a whisker of each other along a whole quadrant, so their boxes overlap everywhere and the subdivision explores an enormous number of leaf pairs to return 3 points per pair. Needs the finder to recognize that two curves run together within tolerance over a stretch, rather than bisecting its way along it.",
        "union-is-sum-of-its-three-parts": "Correct now, but slow: ~9.4s per operation, all of it in intersection finding. The arc and the cubic approximating it stay within a whisker of each other along a whole quadrant, so their boxes overlap everywhere and the subdivision explores an enormous number of leaf pairs to return 3 points per pair. Needs the finder to recognize that two curves run together within tolerance over a stretch, rather than bisecting its way along it."
    },
    "coincident/07-identical-circle-rotated-start": {
        "commutative-exclusion": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "commutative-fracture": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "commutative-intersection": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "commutative-union": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "division-tiles-a": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "exclusion-is-union-minus-intersection": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "fracture-tiles-union": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "partition-orientation": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "parts-fit-inside-the-union": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "union-is-sum-of-its-three-parts": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead."
    },
    "coincident/10-square-vs-degenerate-curves": {
        "commutative-exclusion": "Throws `No outer face of a component found`. A and B are the same square, but B is drawn with segments that are degenerate as curves — a zero-length cubic, a cubic whose controls are collinear with its endpoints, a quadratic whose control sits on its chord, or an arc of zero radius. Those segments are never normalised to lines, so the coincident edges do not merge and the dual graph is left without a traceable outer face. In the production bundle the stripped assert turns this into a raw TypeError.",
        "commutative-fracture": "Throws `No outer face of a component found`. A and B are the same square, but B is drawn with segments that are degenerate as curves — a zero-length cubic, a cubic whose controls are collinear with its endpoints, a quadratic whose control sits on its chord, or an arc of zero radius. Those segments are never normalised to lines, so the coincident edges do not merge and the dual graph is left without a traceable outer face. In the production bundle the stripped assert turns this into a raw TypeError.",
        "commutative-intersection": "Throws `No outer face of a component found`. A and B are the same square, but B is drawn with segments that are degenerate as curves — a zero-length cubic, a cubic whose controls are collinear with its endpoints, a quadratic whose control sits on its chord, or an arc of zero radius. Those segments are never normalised to lines, so the coincident edges do not merge and the dual graph is left without a traceable outer face. In the production bundle the stripped assert turns this into a raw TypeError.",
        "commutative-union": "Throws `No outer face of a component found`. A and B are the same square, but B is drawn with segments that are degenerate as curves — a zero-length cubic, a cubic whose controls are collinear with its endpoints, a quadratic whose control sits on its chord, or an arc of zero radius. Those segments are never normalised to lines, so the coincident edges do not merge and the dual graph is left without a traceable outer face. In the production bundle the stripped assert turns this into a raw TypeError.",
        "division-tiles-a": "Throws `No outer face of a component found`. A and B are the same square, but B is drawn with segments that are degenerate as curves — a zero-length cubic, a cubic whose controls are collinear with its endpoints, a quadratic whose control sits on its chord, or an arc of zero radius. Those segments are never normalised to lines, so the coincident edges do not merge and the dual graph is left without a traceable outer face. In the production bundle the stripped assert turns this into a raw TypeError.",
        "exclusion-is-union-minus-intersection": "Throws `No outer face of a component found`. A and B are the same square, but B is drawn with segments that are degenerate as curves — a zero-length cubic, a cubic whose controls are collinear with its endpoints, a quadratic whose control sits on its chord, or an arc of zero radius. Those segments are never normalised to lines, so the coincident edges do not merge and the dual graph is left without a traceable outer face. In the production bundle the stripped assert turns this into a raw TypeError.",
        "fracture-tiles-union": "Throws `No outer face of a component found`. A and B are the same square, but B is drawn with segments that are degenerate as curves — a zero-length cubic, a cubic whose controls are collinear with its endpoints, a quadratic whose control sits on its chord, or an arc of zero radius. Those segments are never normalised to lines, so the coincident edges do not merge and the dual graph is left without a traceable outer face. In the production bundle the stripped assert turns this into a raw TypeError.",
        "partition-orientation": "Throws `No outer face of a component found`. A and B are the same square, but B is drawn with segments that are degenerate as curves — a zero-length cubic, a cubic whose controls are collinear with its endpoints, a quadratic whose control sits on its chord, or an arc of zero radius. Those segments are never normalised to lines, so the coincident edges do not merge and the dual graph is left without a traceable outer face. In the production bundle the stripped assert turns this into a raw TypeError.",
        "parts-fit-inside-the-union": "Throws `No outer face of a component found`. A and B are the same square, but B is drawn with segments that are degenerate as curves — a zero-length cubic, a cubic whose controls are collinear with its endpoints, a quadratic whose control sits on its chord, or an arc of zero radius. Those segments are never normalised to lines, so the coincident edges do not merge and the dual graph is left without a traceable outer face. In the production bundle the stripped assert turns this into a raw TypeError.",
        "union-is-sum-of-its-three-parts": "Throws `No outer face of a component found`. A and B are the same square, but B is drawn with segments that are degenerate as curves — a zero-length cubic, a cubic whose controls are collinear with its endpoints, a quadratic whose control sits on its chord, or an arc of zero radius. Those segments are never normalised to lines, so the coincident edges do not merge and the dual graph is left without a traceable outer face. In the production bundle the stripped assert turns this into a raw TypeError."
    },
    "coincident/11-circle-vs-overlong-radii": {
        "commutative-exclusion": "Over the 5s budget (5.4s to 21.2s per operation), all of it in intersection finding, and for the same reason as `coincident/04-arc-vs-cubic-circle`: two curves run together within tolerance over a whole stretch, so their boxes overlap everywhere and the subdivision bisects its way along them. Correctness is not assessed here because the budget check short-circuits ahead of it — but Fracture returns a single face for what should be two (the disc and the wedge sharing the rim), so the shared curved boundary is being dissolved rather than kept as an edge. Same root cause as `touching/09-arc-shared-full`.",
        "commutative-fracture": "Over the 5s budget (5.4s to 21.2s per operation), all of it in intersection finding, and for the same reason as `coincident/04-arc-vs-cubic-circle`: two curves run together within tolerance over a whole stretch, so their boxes overlap everywhere and the subdivision bisects its way along them. Correctness is not assessed here because the budget check short-circuits ahead of it — but Fracture returns a single face for what should be two (the disc and the wedge sharing the rim), so the shared curved boundary is being dissolved rather than kept as an edge. Same root cause as `touching/09-arc-shared-full`.",
        "commutative-intersection": "Over the 5s budget (5.4s to 21.2s per operation), all of it in intersection finding, and for the same reason as `coincident/04-arc-vs-cubic-circle`: two curves run together within tolerance over a whole stretch, so their boxes overlap everywhere and the subdivision bisects its way along them. Correctness is not assessed here because the budget check short-circuits ahead of it — but Fracture returns a single face for what should be two (the disc and the wedge sharing the rim), so the shared curved boundary is being dissolved rather than kept as an edge. Same root cause as `touching/09-arc-shared-full`.",
        "commutative-union": "Over the 5s budget (5.4s to 21.2s per operation), all of it in intersection finding, and for the same reason as `coincident/04-arc-vs-cubic-circle`: two curves run together within tolerance over a whole stretch, so their boxes overlap everywhere and the subdivision bisects its way along them. Correctness is not assessed here because the budget check short-circuits ahead of it — but Fracture returns a single face for what should be two (the disc and the wedge sharing the rim), so the shared curved boundary is being dissolved rather than kept as an edge. Same root cause as `touching/09-arc-shared-full`.",
        "division-tiles-a": "Over the 5s budget (5.4s to 21.2s per operation), all of it in intersection finding, and for the same reason as `coincident/04-arc-vs-cubic-circle`: two curves run together within tolerance over a whole stretch, so their boxes overlap everywhere and the subdivision bisects its way along them. Correctness is not assessed here because the budget check short-circuits ahead of it — but Fracture returns a single face for what should be two (the disc and the wedge sharing the rim), so the shared curved boundary is being dissolved rather than kept as an edge. Same root cause as `touching/09-arc-shared-full`.",
        "exclusion-is-union-minus-intersection": "Over the 5s budget (5.4s to 21.2s per operation), all of it in intersection finding, and for the same reason as `coincident/04-arc-vs-cubic-circle`: two curves run together within tolerance over a whole stretch, so their boxes overlap everywhere and the subdivision bisects its way along them. Correctness is not assessed here because the budget check short-circuits ahead of it — but Fracture returns a single face for what should be two (the disc and the wedge sharing the rim), so the shared curved boundary is being dissolved rather than kept as an edge. Same root cause as `touching/09-arc-shared-full`.",
        "fracture-tiles-union": "Over the 5s budget (5.4s to 21.2s per operation), all of it in intersection finding, and for the same reason as `coincident/04-arc-vs-cubic-circle`: two curves run together within tolerance over a whole stretch, so their boxes overlap everywhere and the subdivision bisects its way along them. Correctness is not assessed here because the budget check short-circuits ahead of it — but Fracture returns a single face for what should be two (the disc and the wedge sharing the rim), so the shared curved boundary is being dissolved rather than kept as an edge. Same root cause as `touching/09-arc-shared-full`.",
        "partition-orientation": "Over the 5s budget (5.4s to 21.2s per operation), all of it in intersection finding, and for the same reason as `coincident/04-arc-vs-cubic-circle`: two curves run together within tolerance over a whole stretch, so their boxes overlap everywhere and the subdivision bisects its way along them. Correctness is not assessed here because the budget check short-circuits ahead of it — but Fracture returns a single face for what should be two (the disc and the wedge sharing the rim), so the shared curved boundary is being dissolved rather than kept as an edge. Same root cause as `touching/09-arc-shared-full`.",
        "parts-fit-inside-the-union": "Over the 5s budget (5.4s to 21.2s per operation), all of it in intersection finding, and for the same reason as `coincident/04-arc-vs-cubic-circle`: two curves run together within tolerance over a whole stretch, so their boxes overlap everywhere and the subdivision bisects its way along them. Correctness is not assessed here because the budget check short-circuits ahead of it — but Fracture returns a single face for what should be two (the disc and the wedge sharing the rim), so the shared curved boundary is being dissolved rather than kept as an edge. Same root cause as `touching/09-arc-shared-full`.",
        "union-is-sum-of-its-three-parts": "Over the 5s budget (5.4s to 21.2s per operation), all of it in intersection finding, and for the same reason as `coincident/04-arc-vs-cubic-circle`: two curves run together within tolerance over a whole stretch, so their boxes overlap everywhere and the subdivision bisects its way along them. Correctness is not assessed here because the budget check short-circuits ahead of it — but Fracture returns a single face for what should be two (the disc and the wedge sharing the rim), so the shared curved boundary is being dissolved rather than kept as an edge. Same root cause as `touching/09-arc-shared-full`."
    },
    "coincident/12-square-vs-zero-radius-arc": {
        "commutative-exclusion": "Throws `No outer face of a component found`. A and B are the same square, but B is drawn with segments that are degenerate as curves — a zero-length cubic, a cubic whose controls are collinear with its endpoints, a quadratic whose control sits on its chord, or an arc of zero radius. Those segments are never normalised to lines, so the coincident edges do not merge and the dual graph is left without a traceable outer face. In the production bundle the stripped assert turns this into a raw TypeError.",
        "commutative-fracture": "Throws `No outer face of a component found`. A and B are the same square, but B is drawn with segments that are degenerate as curves — a zero-length cubic, a cubic whose controls are collinear with its endpoints, a quadratic whose control sits on its chord, or an arc of zero radius. Those segments are never normalised to lines, so the coincident edges do not merge and the dual graph is left without a traceable outer face. In the production bundle the stripped assert turns this into a raw TypeError.",
        "commutative-intersection": "Throws `No outer face of a component found`. A and B are the same square, but B is drawn with segments that are degenerate as curves — a zero-length cubic, a cubic whose controls are collinear with its endpoints, a quadratic whose control sits on its chord, or an arc of zero radius. Those segments are never normalised to lines, so the coincident edges do not merge and the dual graph is left without a traceable outer face. In the production bundle the stripped assert turns this into a raw TypeError.",
        "commutative-union": "Throws `No outer face of a component found`. A and B are the same square, but B is drawn with segments that are degenerate as curves — a zero-length cubic, a cubic whose controls are collinear with its endpoints, a quadratic whose control sits on its chord, or an arc of zero radius. Those segments are never normalised to lines, so the coincident edges do not merge and the dual graph is left without a traceable outer face. In the production bundle the stripped assert turns this into a raw TypeError.",
        "division-tiles-a": "Throws `No outer face of a component found`. A and B are the same square, but B is drawn with segments that are degenerate as curves — a zero-length cubic, a cubic whose controls are collinear with its endpoints, a quadratic whose control sits on its chord, or an arc of zero radius. Those segments are never normalised to lines, so the coincident edges do not merge and the dual graph is left without a traceable outer face. In the production bundle the stripped assert turns this into a raw TypeError.",
        "exclusion-is-union-minus-intersection": "Throws `No outer face of a component found`. A and B are the same square, but B is drawn with segments that are degenerate as curves — a zero-length cubic, a cubic whose controls are collinear with its endpoints, a quadratic whose control sits on its chord, or an arc of zero radius. Those segments are never normalised to lines, so the coincident edges do not merge and the dual graph is left without a traceable outer face. In the production bundle the stripped assert turns this into a raw TypeError.",
        "fracture-tiles-union": "Throws `No outer face of a component found`. A and B are the same square, but B is drawn with segments that are degenerate as curves — a zero-length cubic, a cubic whose controls are collinear with its endpoints, a quadratic whose control sits on its chord, or an arc of zero radius. Those segments are never normalised to lines, so the coincident edges do not merge and the dual graph is left without a traceable outer face. In the production bundle the stripped assert turns this into a raw TypeError.",
        "partition-orientation": "Throws `No outer face of a component found`. A and B are the same square, but B is drawn with segments that are degenerate as curves — a zero-length cubic, a cubic whose controls are collinear with its endpoints, a quadratic whose control sits on its chord, or an arc of zero radius. Those segments are never normalised to lines, so the coincident edges do not merge and the dual graph is left without a traceable outer face. In the production bundle the stripped assert turns this into a raw TypeError.",
        "parts-fit-inside-the-union": "Throws `No outer face of a component found`. A and B are the same square, but B is drawn with segments that are degenerate as curves — a zero-length cubic, a cubic whose controls are collinear with its endpoints, a quadratic whose control sits on its chord, or an arc of zero radius. Those segments are never normalised to lines, so the coincident edges do not merge and the dual graph is left without a traceable outer face. In the production bundle the stripped assert turns this into a raw TypeError.",
        "union-is-sum-of-its-three-parts": "Throws `No outer face of a component found`. A and B are the same square, but B is drawn with segments that are degenerate as curves — a zero-length cubic, a cubic whose controls are collinear with its endpoints, a quadratic whose control sits on its chord, or an arc of zero radius. Those segments are never normalised to lines, so the coincident edges do not merge and the dual graph is left without a traceable outer face. In the production bundle the stripped assert turns this into a raw TypeError."
    },
    "conditioning/01-overlap-circles-small-and-far": {
        "exclusion-is-union-minus-intersection": "Two overlapping circles are treated as disjoint under the `small-and-far` conditioning (a scene 2e-3 across at an offset of 1e6). |A u B| comes out as 3.14e-6, one circle, while the three parts sum to 9.42e-6, three circles; the raster oracle disagrees on 7% of compared pixels. Same precision limit as `conditioning/03-edge-shared-full-small-and-far`, and the same note applies: each conditioning passes on its own.",
        "fracture-tiles-union": "Two overlapping circles are treated as disjoint under the `small-and-far` conditioning (a scene 2e-3 across at an offset of 1e6). |A u B| comes out as 3.14e-6, one circle, while the three parts sum to 9.42e-6, three circles; the raster oracle disagrees on 7% of compared pixels. Same precision limit as `conditioning/03-edge-shared-full-small-and-far`, and the same note applies: each conditioning passes on its own.",
        "partition-orientation": "Two overlapping circles are treated as disjoint under the `small-and-far` conditioning (a scene 2e-3 across at an offset of 1e6). |A u B| comes out as 3.14e-6, one circle, while the three parts sum to 9.42e-6, three circles; the raster oracle disagrees on 7% of compared pixels. Same precision limit as `conditioning/03-edge-shared-full-small-and-far`, and the same note applies: each conditioning passes on its own.",
        "union-is-sum-of-its-three-parts": "Two overlapping circles are treated as disjoint under the `small-and-far` conditioning (a scene 2e-3 across at an offset of 1e6). |A u B| comes out as 3.14e-6, one circle, while the three parts sum to 9.42e-6, three circles; the raster oracle disagrees on 7% of compared pixels. Same precision limit as `conditioning/03-edge-shared-full-small-and-far`, and the same note applies: each conditioning passes on its own."
    },
    "conditioning/03-edge-shared-full-small-and-far": {
        "commutative-exclusion": "Throws `No outer face of a component found` under the `small-and-far` conditioning — a scene 2e-3 across sitting at an offset of 1e6, where neighbouring doubles are ~2e-10 apart. Each conditioning on its own passes (`-small` and `-far` are both green), so it is the combination that loses the precision. `config.ts` is explicit that a large offset is a known separate problem that widening tolerances would conceal rather than cure, so this documents the limit rather than proposing to move it. In the production bundle it throws a raw TypeError from `WeakSet.add` in `flagFaces`.",
        "commutative-fracture": "Throws `No outer face of a component found` under the `small-and-far` conditioning — a scene 2e-3 across sitting at an offset of 1e6, where neighbouring doubles are ~2e-10 apart. Each conditioning on its own passes (`-small` and `-far` are both green), so it is the combination that loses the precision. `config.ts` is explicit that a large offset is a known separate problem that widening tolerances would conceal rather than cure, so this documents the limit rather than proposing to move it. In the production bundle it throws a raw TypeError from `WeakSet.add` in `flagFaces`.",
        "commutative-intersection": "Throws `No outer face of a component found` under the `small-and-far` conditioning — a scene 2e-3 across sitting at an offset of 1e6, where neighbouring doubles are ~2e-10 apart. Each conditioning on its own passes (`-small` and `-far` are both green), so it is the combination that loses the precision. `config.ts` is explicit that a large offset is a known separate problem that widening tolerances would conceal rather than cure, so this documents the limit rather than proposing to move it. In the production bundle it throws a raw TypeError from `WeakSet.add` in `flagFaces`.",
        "commutative-union": "Throws `No outer face of a component found` under the `small-and-far` conditioning — a scene 2e-3 across sitting at an offset of 1e6, where neighbouring doubles are ~2e-10 apart. Each conditioning on its own passes (`-small` and `-far` are both green), so it is the combination that loses the precision. `config.ts` is explicit that a large offset is a known separate problem that widening tolerances would conceal rather than cure, so this documents the limit rather than proposing to move it. In the production bundle it throws a raw TypeError from `WeakSet.add` in `flagFaces`.",
        "division-tiles-a": "Throws `No outer face of a component found` under the `small-and-far` conditioning — a scene 2e-3 across sitting at an offset of 1e6, where neighbouring doubles are ~2e-10 apart. Each conditioning on its own passes (`-small` and `-far` are both green), so it is the combination that loses the precision. `config.ts` is explicit that a large offset is a known separate problem that widening tolerances would conceal rather than cure, so this documents the limit rather than proposing to move it. In the production bundle it throws a raw TypeError from `WeakSet.add` in `flagFaces`.",
        "exclusion-is-union-minus-intersection": "Throws `No outer face of a component found` under the `small-and-far` conditioning — a scene 2e-3 across sitting at an offset of 1e6, where neighbouring doubles are ~2e-10 apart. Each conditioning on its own passes (`-small` and `-far` are both green), so it is the combination that loses the precision. `config.ts` is explicit that a large offset is a known separate problem that widening tolerances would conceal rather than cure, so this documents the limit rather than proposing to move it. In the production bundle it throws a raw TypeError from `WeakSet.add` in `flagFaces`.",
        "fracture-tiles-union": "Throws `No outer face of a component found` under the `small-and-far` conditioning — a scene 2e-3 across sitting at an offset of 1e6, where neighbouring doubles are ~2e-10 apart. Each conditioning on its own passes (`-small` and `-far` are both green), so it is the combination that loses the precision. `config.ts` is explicit that a large offset is a known separate problem that widening tolerances would conceal rather than cure, so this documents the limit rather than proposing to move it. In the production bundle it throws a raw TypeError from `WeakSet.add` in `flagFaces`.",
        "partition-orientation": "Throws `No outer face of a component found` under the `small-and-far` conditioning — a scene 2e-3 across sitting at an offset of 1e6, where neighbouring doubles are ~2e-10 apart. Each conditioning on its own passes (`-small` and `-far` are both green), so it is the combination that loses the precision. `config.ts` is explicit that a large offset is a known separate problem that widening tolerances would conceal rather than cure, so this documents the limit rather than proposing to move it. In the production bundle it throws a raw TypeError from `WeakSet.add` in `flagFaces`.",
        "parts-fit-inside-the-union": "Throws `No outer face of a component found` under the `small-and-far` conditioning — a scene 2e-3 across sitting at an offset of 1e6, where neighbouring doubles are ~2e-10 apart. Each conditioning on its own passes (`-small` and `-far` are both green), so it is the combination that loses the precision. `config.ts` is explicit that a large offset is a known separate problem that widening tolerances would conceal rather than cure, so this documents the limit rather than proposing to move it. In the production bundle it throws a raw TypeError from `WeakSet.add` in `flagFaces`.",
        "union-is-sum-of-its-three-parts": "Throws `No outer face of a component found` under the `small-and-far` conditioning — a scene 2e-3 across sitting at an offset of 1e6, where neighbouring doubles are ~2e-10 apart. Each conditioning on its own passes (`-small` and `-far` are both green), so it is the combination that loses the precision. `config.ts` is explicit that a large offset is a known separate problem that widening tolerances would conceal rather than cure, so this documents the limit rather than proposing to move it. In the production bundle it throws a raw TypeError from `WeakSet.add` in `flagFaces`."
    },
    "conditioning/04-arc-shared-full-far": {
        "commutative-exclusion": "Over the 5s budget (5.4s to 21.2s per operation), all of it in intersection finding, and for the same reason as `coincident/04-arc-vs-cubic-circle`: two curves run together within tolerance over a whole stretch, so their boxes overlap everywhere and the subdivision bisects its way along them. Correctness is not assessed here because the budget check short-circuits ahead of it — but Fracture returns a single face for what should be two (the disc and the wedge sharing the rim), so the shared curved boundary is being dissolved rather than kept as an edge. Same root cause as `touching/09-arc-shared-full`.",
        "commutative-fracture": "Over the 5s budget (5.4s to 21.2s per operation), all of it in intersection finding, and for the same reason as `coincident/04-arc-vs-cubic-circle`: two curves run together within tolerance over a whole stretch, so their boxes overlap everywhere and the subdivision bisects its way along them. Correctness is not assessed here because the budget check short-circuits ahead of it — but Fracture returns a single face for what should be two (the disc and the wedge sharing the rim), so the shared curved boundary is being dissolved rather than kept as an edge. Same root cause as `touching/09-arc-shared-full`.",
        "commutative-intersection": "Over the 5s budget (5.4s to 21.2s per operation), all of it in intersection finding, and for the same reason as `coincident/04-arc-vs-cubic-circle`: two curves run together within tolerance over a whole stretch, so their boxes overlap everywhere and the subdivision bisects its way along them. Correctness is not assessed here because the budget check short-circuits ahead of it — but Fracture returns a single face for what should be two (the disc and the wedge sharing the rim), so the shared curved boundary is being dissolved rather than kept as an edge. Same root cause as `touching/09-arc-shared-full`.",
        "commutative-union": "Over the 5s budget (5.4s to 21.2s per operation), all of it in intersection finding, and for the same reason as `coincident/04-arc-vs-cubic-circle`: two curves run together within tolerance over a whole stretch, so their boxes overlap everywhere and the subdivision bisects its way along them. Correctness is not assessed here because the budget check short-circuits ahead of it — but Fracture returns a single face for what should be two (the disc and the wedge sharing the rim), so the shared curved boundary is being dissolved rather than kept as an edge. Same root cause as `touching/09-arc-shared-full`.",
        "division-tiles-a": "Over the 5s budget (5.4s to 21.2s per operation), all of it in intersection finding, and for the same reason as `coincident/04-arc-vs-cubic-circle`: two curves run together within tolerance over a whole stretch, so their boxes overlap everywhere and the subdivision bisects its way along them. Correctness is not assessed here because the budget check short-circuits ahead of it — but Fracture returns a single face for what should be two (the disc and the wedge sharing the rim), so the shared curved boundary is being dissolved rather than kept as an edge. Same root cause as `touching/09-arc-shared-full`.",
        "exclusion-is-union-minus-intersection": "Over the 5s budget (5.4s to 21.2s per operation), all of it in intersection finding, and for the same reason as `coincident/04-arc-vs-cubic-circle`: two curves run together within tolerance over a whole stretch, so their boxes overlap everywhere and the subdivision bisects its way along them. Correctness is not assessed here because the budget check short-circuits ahead of it — but Fracture returns a single face for what should be two (the disc and the wedge sharing the rim), so the shared curved boundary is being dissolved rather than kept as an edge. Same root cause as `touching/09-arc-shared-full`.",
        "fracture-tiles-union": "Over the 5s budget (5.4s to 21.2s per operation), all of it in intersection finding, and for the same reason as `coincident/04-arc-vs-cubic-circle`: two curves run together within tolerance over a whole stretch, so their boxes overlap everywhere and the subdivision bisects its way along them. Correctness is not assessed here because the budget check short-circuits ahead of it — but Fracture returns a single face for what should be two (the disc and the wedge sharing the rim), so the shared curved boundary is being dissolved rather than kept as an edge. Same root cause as `touching/09-arc-shared-full`.",
        "partition-orientation": "Over the 5s budget (5.4s to 21.2s per operation), all of it in intersection finding, and for the same reason as `coincident/04-arc-vs-cubic-circle`: two curves run together within tolerance over a whole stretch, so their boxes overlap everywhere and the subdivision bisects its way along them. Correctness is not assessed here because the budget check short-circuits ahead of it — but Fracture returns a single face for what should be two (the disc and the wedge sharing the rim), so the shared curved boundary is being dissolved rather than kept as an edge. Same root cause as `touching/09-arc-shared-full`.",
        "parts-fit-inside-the-union": "Over the 5s budget (5.4s to 21.2s per operation), all of it in intersection finding, and for the same reason as `coincident/04-arc-vs-cubic-circle`: two curves run together within tolerance over a whole stretch, so their boxes overlap everywhere and the subdivision bisects its way along them. Correctness is not assessed here because the budget check short-circuits ahead of it — but Fracture returns a single face for what should be two (the disc and the wedge sharing the rim), so the shared curved boundary is being dissolved rather than kept as an edge. Same root cause as `touching/09-arc-shared-full`.",
        "union-is-sum-of-its-three-parts": "Over the 5s budget (5.4s to 21.2s per operation), all of it in intersection finding, and for the same reason as `coincident/04-arc-vs-cubic-circle`: two curves run together within tolerance over a whole stretch, so their boxes overlap everywhere and the subdivision bisects its way along them. Correctness is not assessed here because the budget check short-circuits ahead of it — but Fracture returns a single face for what should be two (the disc and the wedge sharing the rim), so the shared curved boundary is being dissolved rather than kept as an edge. Same root cause as `touching/09-arc-shared-full`."
    },
    "conditioning/04-arc-shared-full-large": {
        "commutative-exclusion": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "commutative-fracture": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "commutative-intersection": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "commutative-union": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "division-tiles-a": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "exclusion-is-union-minus-intersection": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "fracture-tiles-union": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "partition-orientation": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "parts-fit-inside-the-union": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "union-is-sum-of-its-three-parts": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead."
    },
    "conditioning/04-arc-shared-full-rotated": {
        "commutative-exclusion": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "commutative-fracture": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "commutative-intersection": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "commutative-union": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "division-tiles-a": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "exclusion-is-union-minus-intersection": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "fracture-tiles-union": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "partition-orientation": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "parts-fit-inside-the-union": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "union-is-sum-of-its-three-parts": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead."
    },
    "conditioning/04-arc-shared-full-small": {
        "commutative-exclusion": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "commutative-fracture": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "commutative-intersection": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "commutative-union": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "division-tiles-a": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "exclusion-is-union-minus-intersection": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "fracture-tiles-union": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "partition-orientation": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "parts-fit-inside-the-union": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "union-is-sum-of-its-three-parts": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead."
    },
    "conditioning/04-arc-shared-full-small-and-far": {
        "commutative-exclusion": "Throws `No outer face of a component found` under the `small-and-far` conditioning — a scene 2e-3 across sitting at an offset of 1e6, where neighbouring doubles are ~2e-10 apart. Each conditioning on its own passes (`-small` and `-far` are both green), so it is the combination that loses the precision. `config.ts` is explicit that a large offset is a known separate problem that widening tolerances would conceal rather than cure, so this documents the limit rather than proposing to move it. In the production bundle it throws a raw TypeError from `WeakSet.add` in `flagFaces`.",
        "commutative-fracture": "Throws `No outer face of a component found` under the `small-and-far` conditioning — a scene 2e-3 across sitting at an offset of 1e6, where neighbouring doubles are ~2e-10 apart. Each conditioning on its own passes (`-small` and `-far` are both green), so it is the combination that loses the precision. `config.ts` is explicit that a large offset is a known separate problem that widening tolerances would conceal rather than cure, so this documents the limit rather than proposing to move it. In the production bundle it throws a raw TypeError from `WeakSet.add` in `flagFaces`.",
        "commutative-intersection": "Throws `No outer face of a component found` under the `small-and-far` conditioning — a scene 2e-3 across sitting at an offset of 1e6, where neighbouring doubles are ~2e-10 apart. Each conditioning on its own passes (`-small` and `-far` are both green), so it is the combination that loses the precision. `config.ts` is explicit that a large offset is a known separate problem that widening tolerances would conceal rather than cure, so this documents the limit rather than proposing to move it. In the production bundle it throws a raw TypeError from `WeakSet.add` in `flagFaces`.",
        "commutative-union": "Throws `No outer face of a component found` under the `small-and-far` conditioning — a scene 2e-3 across sitting at an offset of 1e6, where neighbouring doubles are ~2e-10 apart. Each conditioning on its own passes (`-small` and `-far` are both green), so it is the combination that loses the precision. `config.ts` is explicit that a large offset is a known separate problem that widening tolerances would conceal rather than cure, so this documents the limit rather than proposing to move it. In the production bundle it throws a raw TypeError from `WeakSet.add` in `flagFaces`.",
        "division-tiles-a": "Throws `No outer face of a component found` under the `small-and-far` conditioning — a scene 2e-3 across sitting at an offset of 1e6, where neighbouring doubles are ~2e-10 apart. Each conditioning on its own passes (`-small` and `-far` are both green), so it is the combination that loses the precision. `config.ts` is explicit that a large offset is a known separate problem that widening tolerances would conceal rather than cure, so this documents the limit rather than proposing to move it. In the production bundle it throws a raw TypeError from `WeakSet.add` in `flagFaces`.",
        "exclusion-is-union-minus-intersection": "Throws `No outer face of a component found` under the `small-and-far` conditioning — a scene 2e-3 across sitting at an offset of 1e6, where neighbouring doubles are ~2e-10 apart. Each conditioning on its own passes (`-small` and `-far` are both green), so it is the combination that loses the precision. `config.ts` is explicit that a large offset is a known separate problem that widening tolerances would conceal rather than cure, so this documents the limit rather than proposing to move it. In the production bundle it throws a raw TypeError from `WeakSet.add` in `flagFaces`.",
        "fracture-tiles-union": "Throws `No outer face of a component found` under the `small-and-far` conditioning — a scene 2e-3 across sitting at an offset of 1e6, where neighbouring doubles are ~2e-10 apart. Each conditioning on its own passes (`-small` and `-far` are both green), so it is the combination that loses the precision. `config.ts` is explicit that a large offset is a known separate problem that widening tolerances would conceal rather than cure, so this documents the limit rather than proposing to move it. In the production bundle it throws a raw TypeError from `WeakSet.add` in `flagFaces`.",
        "partition-orientation": "Throws `No outer face of a component found` under the `small-and-far` conditioning — a scene 2e-3 across sitting at an offset of 1e6, where neighbouring doubles are ~2e-10 apart. Each conditioning on its own passes (`-small` and `-far` are both green), so it is the combination that loses the precision. `config.ts` is explicit that a large offset is a known separate problem that widening tolerances would conceal rather than cure, so this documents the limit rather than proposing to move it. In the production bundle it throws a raw TypeError from `WeakSet.add` in `flagFaces`.",
        "parts-fit-inside-the-union": "Throws `No outer face of a component found` under the `small-and-far` conditioning — a scene 2e-3 across sitting at an offset of 1e6, where neighbouring doubles are ~2e-10 apart. Each conditioning on its own passes (`-small` and `-far` are both green), so it is the combination that loses the precision. `config.ts` is explicit that a large offset is a known separate problem that widening tolerances would conceal rather than cure, so this documents the limit rather than proposing to move it. In the production bundle it throws a raw TypeError from `WeakSet.add` in `flagFaces`.",
        "union-is-sum-of-its-three-parts": "Throws `No outer face of a component found` under the `small-and-far` conditioning — a scene 2e-3 across sitting at an offset of 1e6, where neighbouring doubles are ~2e-10 apart. Each conditioning on its own passes (`-small` and `-far` are both green), so it is the combination that loses the precision. `config.ts` is explicit that a large offset is a known separate problem that widening tolerances would conceal rather than cure, so this documents the limit rather than proposing to move it. In the production bundle it throws a raw TypeError from `WeakSet.add` in `flagFaces`."
    },
    "conditioning/04-arc-shared-full-tiny": {
        "commutative-exclusion": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "commutative-fracture": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "commutative-intersection": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "commutative-union": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "division-tiles-a": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "exclusion-is-union-minus-intersection": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "fracture-tiles-union": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "partition-orientation": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "parts-fit-inside-the-union": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "union-is-sum-of-its-three-parts": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead."
    },
    "touching/08-arc-shared-partial": {
        "commutative-exclusion": "Over the 5s budget (5.4s to 21.2s per operation), all of it in intersection finding, and for the same reason as `coincident/04-arc-vs-cubic-circle`: two curves run together within tolerance over a whole stretch, so their boxes overlap everywhere and the subdivision bisects its way along them. Correctness is not assessed here because the budget check short-circuits ahead of it — but Fracture returns a single face for what should be two (the disc and the wedge sharing the rim), so the shared curved boundary is being dissolved rather than kept as an edge. Same root cause as `touching/09-arc-shared-full`.",
        "commutative-fracture": "Over the 5s budget (5.4s to 21.2s per operation), all of it in intersection finding, and for the same reason as `coincident/04-arc-vs-cubic-circle`: two curves run together within tolerance over a whole stretch, so their boxes overlap everywhere and the subdivision bisects its way along them. Correctness is not assessed here because the budget check short-circuits ahead of it — but Fracture returns a single face for what should be two (the disc and the wedge sharing the rim), so the shared curved boundary is being dissolved rather than kept as an edge. Same root cause as `touching/09-arc-shared-full`.",
        "commutative-intersection": "Over the 5s budget (5.4s to 21.2s per operation), all of it in intersection finding, and for the same reason as `coincident/04-arc-vs-cubic-circle`: two curves run together within tolerance over a whole stretch, so their boxes overlap everywhere and the subdivision bisects its way along them. Correctness is not assessed here because the budget check short-circuits ahead of it — but Fracture returns a single face for what should be two (the disc and the wedge sharing the rim), so the shared curved boundary is being dissolved rather than kept as an edge. Same root cause as `touching/09-arc-shared-full`.",
        "commutative-union": "Over the 5s budget (5.4s to 21.2s per operation), all of it in intersection finding, and for the same reason as `coincident/04-arc-vs-cubic-circle`: two curves run together within tolerance over a whole stretch, so their boxes overlap everywhere and the subdivision bisects its way along them. Correctness is not assessed here because the budget check short-circuits ahead of it — but Fracture returns a single face for what should be two (the disc and the wedge sharing the rim), so the shared curved boundary is being dissolved rather than kept as an edge. Same root cause as `touching/09-arc-shared-full`.",
        "division-tiles-a": "Over the 5s budget (5.4s to 21.2s per operation), all of it in intersection finding, and for the same reason as `coincident/04-arc-vs-cubic-circle`: two curves run together within tolerance over a whole stretch, so their boxes overlap everywhere and the subdivision bisects its way along them. Correctness is not assessed here because the budget check short-circuits ahead of it — but Fracture returns a single face for what should be two (the disc and the wedge sharing the rim), so the shared curved boundary is being dissolved rather than kept as an edge. Same root cause as `touching/09-arc-shared-full`.",
        "exclusion-is-union-minus-intersection": "Over the 5s budget (5.4s to 21.2s per operation), all of it in intersection finding, and for the same reason as `coincident/04-arc-vs-cubic-circle`: two curves run together within tolerance over a whole stretch, so their boxes overlap everywhere and the subdivision bisects its way along them. Correctness is not assessed here because the budget check short-circuits ahead of it — but Fracture returns a single face for what should be two (the disc and the wedge sharing the rim), so the shared curved boundary is being dissolved rather than kept as an edge. Same root cause as `touching/09-arc-shared-full`.",
        "fracture-tiles-union": "Over the 5s budget (5.4s to 21.2s per operation), all of it in intersection finding, and for the same reason as `coincident/04-arc-vs-cubic-circle`: two curves run together within tolerance over a whole stretch, so their boxes overlap everywhere and the subdivision bisects its way along them. Correctness is not assessed here because the budget check short-circuits ahead of it — but Fracture returns a single face for what should be two (the disc and the wedge sharing the rim), so the shared curved boundary is being dissolved rather than kept as an edge. Same root cause as `touching/09-arc-shared-full`.",
        "partition-orientation": "Over the 5s budget (5.4s to 21.2s per operation), all of it in intersection finding, and for the same reason as `coincident/04-arc-vs-cubic-circle`: two curves run together within tolerance over a whole stretch, so their boxes overlap everywhere and the subdivision bisects its way along them. Correctness is not assessed here because the budget check short-circuits ahead of it — but Fracture returns a single face for what should be two (the disc and the wedge sharing the rim), so the shared curved boundary is being dissolved rather than kept as an edge. Same root cause as `touching/09-arc-shared-full`.",
        "parts-fit-inside-the-union": "Over the 5s budget (5.4s to 21.2s per operation), all of it in intersection finding, and for the same reason as `coincident/04-arc-vs-cubic-circle`: two curves run together within tolerance over a whole stretch, so their boxes overlap everywhere and the subdivision bisects its way along them. Correctness is not assessed here because the budget check short-circuits ahead of it — but Fracture returns a single face for what should be two (the disc and the wedge sharing the rim), so the shared curved boundary is being dissolved rather than kept as an edge. Same root cause as `touching/09-arc-shared-full`.",
        "union-is-sum-of-its-three-parts": "Over the 5s budget (5.4s to 21.2s per operation), all of it in intersection finding, and for the same reason as `coincident/04-arc-vs-cubic-circle`: two curves run together within tolerance over a whole stretch, so their boxes overlap everywhere and the subdivision bisects its way along them. Correctness is not assessed here because the budget check short-circuits ahead of it — but Fracture returns a single face for what should be two (the disc and the wedge sharing the rim), so the shared curved boundary is being dissolved rather than kept as an edge. Same root cause as `touching/09-arc-shared-full`."
    },
    "touching/09-arc-shared-full": {
        "commutative-exclusion": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "commutative-fracture": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "commutative-intersection": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "commutative-union": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "division-tiles-a": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "exclusion-is-union-minus-intersection": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "fracture-tiles-union": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "partition-orientation": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "parts-fit-inside-the-union": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead.",
        "union-is-sum-of-its-three-parts": "A boundary shared as a curve comes apart. A and B run along the same circle but their vertices interleave, so the shared rim has to be re-split; instead it fragments, and the arrangement ends up with hundreds of outer faces where there should be one (`Multiple outer faces found`). Division returns 264 faces for what should be a single disc. The straight-edge analogue, `touching/05-edge-shared-full`, is handled correctly, so this is specific to coincident *curves*. In the production bundle the same input reaches `computeNestingTree` with a component that has no inner face, and the stripped `assertDefined` lets it throw a raw TypeError out of `boundingBoxArea` instead."
    }
}
