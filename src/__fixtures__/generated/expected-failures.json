{
    "conditioning/03-edge-shared-full-small-and-far": {
        "difference": "Throws `No outer face of a component found` under the `small-and-far` conditioning \u2014 a scene 2e-3 across sitting at an offset of 1e6, where neighbouring doubles are ~2e-10 apart. Each conditioning on its own passes (`-small` and `-far` are both green), so it is the combination that loses the precision. `config.ts` is explicit that a large offset is a known separate problem that widening tolerances would conceal rather than cure, so this documents the limit rather than proposing to move it. In the production bundle it throws a raw TypeError from `WeakSet.add` in `flagFaces`.",
        "division": "Throws `No outer face of a component found` under the `small-and-far` conditioning \u2014 a scene 2e-3 across sitting at an offset of 1e6, where neighbouring doubles are ~2e-10 apart. Each conditioning on its own passes (`-small` and `-far` are both green), so it is the combination that loses the precision. `config.ts` is explicit that a large offset is a known separate problem that widening tolerances would conceal rather than cure, so this documents the limit rather than proposing to move it. In the production bundle it throws a raw TypeError from `WeakSet.add` in `flagFaces`.",
        "exclusion": "Throws `No outer face of a component found` under the `small-and-far` conditioning \u2014 a scene 2e-3 across sitting at an offset of 1e6, where neighbouring doubles are ~2e-10 apart. Each conditioning on its own passes (`-small` and `-far` are both green), so it is the combination that loses the precision. `config.ts` is explicit that a large offset is a known separate problem that widening tolerances would conceal rather than cure, so this documents the limit rather than proposing to move it. In the production bundle it throws a raw TypeError from `WeakSet.add` in `flagFaces`.",
        "fracture": "Throws `No outer face of a component found` under the `small-and-far` conditioning \u2014 a scene 2e-3 across sitting at an offset of 1e6, where neighbouring doubles are ~2e-10 apart. Each conditioning on its own passes (`-small` and `-far` are both green), so it is the combination that loses the precision. `config.ts` is explicit that a large offset is a known separate problem that widening tolerances would conceal rather than cure, so this documents the limit rather than proposing to move it. In the production bundle it throws a raw TypeError from `WeakSet.add` in `flagFaces`.",
        "intersection": "Throws `No outer face of a component found` under the `small-and-far` conditioning \u2014 a scene 2e-3 across sitting at an offset of 1e6, where neighbouring doubles are ~2e-10 apart. Each conditioning on its own passes (`-small` and `-far` are both green), so it is the combination that loses the precision. `config.ts` is explicit that a large offset is a known separate problem that widening tolerances would conceal rather than cure, so this documents the limit rather than proposing to move it. In the production bundle it throws a raw TypeError from `WeakSet.add` in `flagFaces`.",
        "union": "Throws `No outer face of a component found` under the `small-and-far` conditioning \u2014 a scene 2e-3 across sitting at an offset of 1e6, where neighbouring doubles are ~2e-10 apart. Each conditioning on its own passes (`-small` and `-far` are both green), so it is the combination that loses the precision. `config.ts` is explicit that a large offset is a known separate problem that widening tolerances would conceal rather than cure, so this documents the limit rather than proposing to move it. In the production bundle it throws a raw TypeError from `WeakSet.add` in `flagFaces`."
    },
    "conditioning/04-arc-shared-full-small-and-far": {
        "difference": "Throws `No outer face of a component found` under the `small-and-far` conditioning \u2014 a scene 2e-3 across sitting at an offset of 1e6, where neighbouring doubles are ~2e-10 apart. Each conditioning on its own passes (`-small` and `-far` are both green), so it is the combination that loses the precision. `config.ts` is explicit that a large offset is a known separate problem that widening tolerances would conceal rather than cure, so this documents the limit rather than proposing to move it. In the production bundle it throws a raw TypeError from `WeakSet.add` in `flagFaces`.",
        "division": "Throws `No outer face of a component found` under the `small-and-far` conditioning \u2014 a scene 2e-3 across sitting at an offset of 1e6, where neighbouring doubles are ~2e-10 apart. Each conditioning on its own passes (`-small` and `-far` are both green), so it is the combination that loses the precision. `config.ts` is explicit that a large offset is a known separate problem that widening tolerances would conceal rather than cure, so this documents the limit rather than proposing to move it. In the production bundle it throws a raw TypeError from `WeakSet.add` in `flagFaces`.",
        "exclusion": "Throws `No outer face of a component found` under the `small-and-far` conditioning \u2014 a scene 2e-3 across sitting at an offset of 1e6, where neighbouring doubles are ~2e-10 apart. Each conditioning on its own passes (`-small` and `-far` are both green), so it is the combination that loses the precision. `config.ts` is explicit that a large offset is a known separate problem that widening tolerances would conceal rather than cure, so this documents the limit rather than proposing to move it. In the production bundle it throws a raw TypeError from `WeakSet.add` in `flagFaces`.",
        "fracture": "Throws `No outer face of a component found` under the `small-and-far` conditioning \u2014 a scene 2e-3 across sitting at an offset of 1e6, where neighbouring doubles are ~2e-10 apart. Each conditioning on its own passes (`-small` and `-far` are both green), so it is the combination that loses the precision. `config.ts` is explicit that a large offset is a known separate problem that widening tolerances would conceal rather than cure, so this documents the limit rather than proposing to move it. In the production bundle it throws a raw TypeError from `WeakSet.add` in `flagFaces`.",
        "intersection": "Throws `No outer face of a component found` under the `small-and-far` conditioning \u2014 a scene 2e-3 across sitting at an offset of 1e6, where neighbouring doubles are ~2e-10 apart. Each conditioning on its own passes (`-small` and `-far` are both green), so it is the combination that loses the precision. `config.ts` is explicit that a large offset is a known separate problem that widening tolerances would conceal rather than cure, so this documents the limit rather than proposing to move it. In the production bundle it throws a raw TypeError from `WeakSet.add` in `flagFaces`.",
        "union": "Throws `No outer face of a component found` under the `small-and-far` conditioning \u2014 a scene 2e-3 across sitting at an offset of 1e6, where neighbouring doubles are ~2e-10 apart. Each conditioning on its own passes (`-small` and `-far` are both green), so it is the combination that loses the precision. `config.ts` is explicit that a large offset is a known separate problem that widening tolerances would conceal rather than cure, so this documents the limit rather than proposing to move it. In the production bundle it throws a raw TypeError from `WeakSet.add` in `flagFaces`."
    },
    "malformed/duplicate-opposite-winding-boundary-3": {
        "union": "Cancelling opposite-winding copies of A share an edge with B. Forward selection leaves an open boundary and area 1200 instead of 800; swapping inputs changes the answer. path 0 loop starting at segment 0 does not close: ends 4.000e+1 away from its start (tolerance 1.000e-6)",
        "exclusion": "Cancelling opposite-winding copies of A share an edge with B. Forward selection leaves an open boundary and area 1200 instead of 800; swapping inputs changes the answer. path 0 loop starting at segment 0 does not close: ends 4.000e+1 away from its start (tolerance 1.000e-6)",
        "fracture": "Cancelling opposite-winding copies of A share an edge with B. Forward selection leaves an open boundary and area 1200 instead of 800; swapping inputs changes the answer. path 0 loop starting at segment 0 does not close: ends 4.000e+1 away from its start (tolerance 1.000e-6)"
    }
}
